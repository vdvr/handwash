pollSec moet opgeteld worden in timer interrupt

void poll_RPI(void)
{
    if(!rpi_avail)
    {
        if(pollSec >= 120)
        {
            send_msg(POLL_REQUEST,"");
        }
    }
}

void poll_reset(void)           // deze functie oproepen bij elke actie bij atmega
{
    pollSec = 0;
}

